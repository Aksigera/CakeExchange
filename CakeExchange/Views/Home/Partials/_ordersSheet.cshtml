@model IEnumerable<Order>

<table>
    <tr>
        <th>Цена</th>
        <th>Кол-во</th>
        <th>Email</th>
    </tr>

    @foreach (Order order in Model)
    {
        <tr>
            <td>@order.Price.ToString("G29")</td>
            <td>@order.Number</td>
            <td>@order.Email</td>
        </tr>
    }

</table >